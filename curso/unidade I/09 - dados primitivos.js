/**
 * Strings
 * Number
 * Undefined -> undefined != null
 */

const nome = 'Yoruichi';  // string
const num = 10;           // number 
let nada;                 // undefined
let nulo = null;          // nao aponta pra nenhum local na memoria 
let logico = true;        // boleano -> true | false

console.log(typeof(nada));