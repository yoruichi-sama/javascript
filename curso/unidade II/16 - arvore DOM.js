/**
 * | -> O objeto window é a raiz da arvore.
 * | -> Apos a raiz temos o objeto document e outros.
 * | -> Dentro desse document temos a ARVORE DOM.
 * | -> document é pai da arvore <html>.
 * 
 * | -> <h1>Lorem ipsum dolor sit amet</h1>
 * | -> dentro de uma tag H1 temos o seu child que seria
 * | -> o nó de texto (textNode)
 */