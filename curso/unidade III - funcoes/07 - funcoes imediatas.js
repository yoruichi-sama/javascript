/**
 * IIFE -> Immediately invoked function expression
 * 
 * Funçoes anonimas que nao tocam o escopo global.
 */

(function (params){

    const nome = 'yoruichi';

    function foo(params) {
        
    }

})('params');
